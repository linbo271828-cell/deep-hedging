"""Summary table generation — STUB, not yet implemented.

Lane F (reporting/docs) owns this file.
Implement in Phase E (ablations + reporting).

Target public interface:
    build_summary_table(rows: list[SummaryRow]) -> pd.DataFrame
    save_csv(df: pd.DataFrame, path: str) -> None
    save_latex(df: pd.DataFrame, path: str) -> None
"""

from __future__ import annotations


def build_summary_table(rows: list[object]) -> object:
    """Build a summary DataFrame from a list of SummaryRow objects.

    STUB — not yet implemented.
    """
    raise NotImplementedError(
        "build_summary_table is not yet implemented. "
        "See writeup/implementation_plan.md Phase 5."
    )
